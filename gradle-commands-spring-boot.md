# gradle commands to build spring boot app

## build a project
```bash
./gradlew build
```

## clean a spring boot project
```bash
./gradlew clean
```

## building a jar file of project
```bash
./gradlew clean build
```